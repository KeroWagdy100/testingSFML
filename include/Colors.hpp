#pragma once
#include <SFML/Graphics.hpp>

namespace Color {

    const sf::Color TEXT_COLOR   {255, 255, 255}; // Text Color
    const sf::Color BG_COLOR     {  0,   0,   0}; // Background Color

    const sf::Color mainButton     {254, 149,   5}; // Main Button Default Color
    const sf::Color secButton      {165, 165, 165}; // Secondary Button Color
    const sf::Color numButton      { 51,  50,  50}; // Num Button Color

    const sf::Color hoverShift   { 20, 20,  20};
    const sf::Color pressedShift { 30, 30,  30};
};